# Copy this file to .env.local and fill in the real secrets. DO NOT commit .env.local or your real API keys.
# This example uses placeholder variable names and safe example values.
# For local development use a file named `.env.local` (already ignored by git in most setups).
# Example mapping guidance:
# - Client-side VITE_* variables remain in the frontend; server-side functions (Netlify) should use plain ENV names.

# Mailchimp (server-side)
MAILCHIMP_API_KEY=__REPLACE_WITH_YOUR_MAILCHIMP_API_KEY__
MAILCHIMP_LIST_ID=__REPLACE_WITH_YOUR_MAILCHIMP_LIST_ID__
MAILCHIMP_SERVER_PREFIX=__REPLACE_WITH_YOUR_MAILCHIMP_SERVER_PREFIX__

# Airtable (server-side)
AIRTABLE_API_KEY=__REPLACE_WITH_YOUR_AIRTABLE_API_KEY__
AIRTABLE_BASE_ID=__REPLACE_WITH_YOUR_AIRTABLE_BASE_ID__
AIRTABLE_TABLE_NAME=__REPLACE_WITH_YOUR_AIRTABLE_TABLE_NAME__

# Optional: logging
LOG_LEVEL=info # debug|info|warn|error
