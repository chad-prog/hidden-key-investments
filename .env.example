# Add your API keys here and then rename this file to .env
# NEVER commit .env to GitHub!

# Mailchimp API
VITE_MAILCHIMP_API_KEY=your_mailchimp_api_key_here
VITE_MAILCHIMP_SERVER_PREFIX=your_server_prefix_here
VITE_MAILCHIMP_AUDIENCE_ACCREDITED=your_audience_id_here
VITE_MAILCHIMP_AUDIENCE_FIRST_TIME=your_audience_id_here
VITE_MAILCHIMP_AUDIENCE_PASSIVE=your_audience_id_here
VITE_MAILCHIMP_AUDIENCE_TEXAS=your_audience_id_here
VITE_MAILCHIMP_AUDIENCE_GENERAL=your_audience_id_here

# Airtable API
VITE_AIRTABLE_API_KEY=your_airtable_api_key_here
VITE_AIRTABLE_BASE_INVESTOR_LEADS=your_base_id_here
VITE_AIRTABLE_BASE_INVESTOR_INTERACTIONS=your_base_id_here
VITE_AIRTABLE_BASE_PROPERTY_TRACKER=your_base_id_here

# Formspree - Lead capture form submission
VITE_FORMSPREE_FORM_ID=your_form_id_here
# You can also provide the full Formspree endpoint instead of just the form id:
# VITE_FORMSPREE_ENDPOINT=https://formspree.io/f/your_form_id_here

# Supabase - Database (for serverless functions)
# Note: These should be set in Netlify environment variables, not here
# SUPABASE_URL=your_supabase_project_url
# SUPABASE_ANON_KEY=your_supabase_anon_key

# Security Notes
# ==============
# NOTE: Any variable prefixed with VITE_ is embedded in the built client bundle and is PUBLIC.
# Do NOT place secret keys in VITE_ variables. For server-only secrets, store them in Netlify's
# environment variables without the VITE_ prefix and access them from serverless functions.
#
# Demo mode: If VITE_FORMSPREE_ENDPOINT, VITE_MAILCHIMP_API_KEY or VITE_AIRTABLE_API_KEY are
# not set or left as the placeholder strings, the client will operate in a safe demo/no-op mode
# and still show success screens for testing local flows.
#
# For production deployment:
# 1. Set all required environment variables in Netlify's dashboard
# 2. Use GitHub Secrets for CI/CD sensitive data
# 3. Never commit actual API keys to the repository
