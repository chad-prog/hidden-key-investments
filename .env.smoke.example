# Smoke Test Environment Configuration
# Copy this file to .env.smoke and configure for your environment

# Base URL for smoke testing
# Examples:
#   Local: http://localhost:8888
#   Staging: https://staging.hiddenkeyinvestments.com
#   Production: https://hiddenkeyinvestments.com
SMOKE_BASE=https://staging.hiddenkeyinvestments.com

# Origin for CORS validation (defaults to SMOKE_BASE if not set)
SMOKE_ORIGIN=https://staging.hiddenkeyinvestments.com

# Mautic campaign ID for investor welcome flow
# This should match the campaign ID in your Mautic instance
# Example: 1, 2, 3, etc.
MAUTIC_CAMPAIGN_INVESTOR_WELCOME=1

# Optional: Request timeout in milliseconds (default: 30000)
# SMOKE_TIMEOUT=30000

# Optional: Path to write test results JSON (default: ./smoke-test-results.json)
# SMOKE_RESULTS_PATH=./smoke-test-results.json

# Note: In demo mode (when Mautic credentials are not configured),
# the smoke tests will still validate request/response structure
# but won't perform actual CRM operations.
