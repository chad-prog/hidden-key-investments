# Smoke Test Environment Variables Example
# Copy to .env.smoke and fill in actual values for local testing

# Base URL of the deployment to test (required)
SMOKE_BASE=https://staging-hidden-key-investments.netlify.app

# Origin for CORS validation (defaults to SMOKE_BASE if not provided)
SMOKE_ORIGIN=https://hidden-key-investments.netlify.app

# Mautic campaign ID for investor welcome sequence (optional)
# If provided, smoke test will attempt to add the test contact to this campaign
MAUTIC_CAMPAIGN_INVESTOR_WELCOME=123

# These are typically set in GitHub Actions or Netlify env, not in .env.smoke
# MAUTIC_BASE_URL=https://your-mautic-instance.com
# MAUTIC_CLIENT_ID=your_client_id
# MAUTIC_CLIENT_SECRET=your_client_secret
# MAUTIC_WEBHOOK_SECRET=your_webhook_secret
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
